# LangGraph / Azure OpenAI configuration
# Copy to `.env` and fill with your values before running locally.

# Azure OpenAI endpoint (example: https://<your-openai-resource>.openai.azure.com)
AZURE_OPENAI_ENDPOINT=
# The name of the model deployment you created in your Azure OpenAI resource
AZURE_OPENAI_DEPLOYMENT_NAME=

# Optional: use a Service Principal for local auth instead of `az login`
# AZURE_CLIENT_ID=
# AZURE_CLIENT_SECRET=
# AZURE_TENANT_ID=

# Optional: app port
PORT=3000
